/*import { OAuthProvider } from '../../../domain/enums/oauth-provider.enum';

export class OAuthProviderEnumMapper {
  static toPrismaOAuthProvider(value: OAuthProvider): PrismaOAuthProvider {
    switch (value) {
      case 'GITHUB':
        return PrismaOAuthProvider.GITHUB;
      case 'GOOGLE':
        return PrismaOAuthProvider.GOOGLE;
    }
  }
}*/
