/*import { ArrayNotEmpty, IsArray, IsString, IsUUID } from 'class-validator';

export const FILES_PATTERNS = {
  SOFT_DELETE_FILES: { cmd: 'files.soft_delete_files' },
} as const;

export class SoftDeleteFilesInput {
  @IsArray()
  @ArrayNotEmpty()
  @IsUUID('4', { each: true })
  fileIds: string[];

  @IsString()
  ownerId: string;
}*/
